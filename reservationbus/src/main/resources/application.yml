spring:
  datasource:
    url: jdbc:mysql://localhost:3306/reservation-bus
    username: root
    password: aros197206

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect